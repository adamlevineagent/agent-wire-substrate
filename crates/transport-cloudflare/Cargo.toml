[package]
name = "agent-wire-transport-cloudflare"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Cloudflare transport driver implementation for Agent Wire substrate nodes."

[dependencies]
agent-wire-foundation.workspace = true
